/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  EndSeriesMutationRequest,
  EndSeriesMutationResponse,
  EndSeriesPathParams,
} from "../types/EndSeries.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getEndSeriesUrl(series_id: EndSeriesPathParams["series_id"]) {
  const res = { method: "POST", url: `/api/series/${series_id}/end` as const };
  return res;
}

/**
 * @summary End Series
 * {@link /api/series/:series_id/end}
 */
export async function endSeries(
  series_id: EndSeriesPathParams["series_id"],
  data: EndSeriesMutationRequest,
  config: Partial<RequestConfig<EndSeriesMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;

  const res = await request<
    EndSeriesMutationResponse,
    ResponseErrorConfig<Error>,
    EndSeriesMutationRequest
  >({
    method: "POST",
    url: getEndSeriesUrl(series_id).url.toString(),
    data: requestData,
    ...requestConfig,
  });
  return res.data;
}
