/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  UpdateSeriesMutationRequest,
  UpdateSeriesMutationResponse,
  UpdateSeriesPathParams,
  UpdateSeriesQueryParams,
} from "../types/UpdateSeries.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getUpdateSeriesUrl(series_id: UpdateSeriesPathParams["series_id"]) {
  const res = { method: "PUT", url: `/api/series/${series_id}` as const };
  return res;
}

/**
 * @summary Update Series
 * {@link /api/series/:series_id}
 */
export async function updateSeries(
  series_id: UpdateSeriesPathParams["series_id"],
  data: UpdateSeriesMutationRequest,
  params?: UpdateSeriesQueryParams,
  config: Partial<RequestConfig<UpdateSeriesMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;

  const res = await request<
    UpdateSeriesMutationResponse,
    ResponseErrorConfig<Error>,
    UpdateSeriesMutationRequest
  >({
    method: "PUT",
    url: getUpdateSeriesUrl(series_id).url.toString(),
    params,
    data: requestData,
    ...requestConfig,
  });
  return res.data;
}
