/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  MarkNotificationReadMutationResponse,
  MarkNotificationReadPathParams,
} from "../types/MarkNotificationRead.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getMarkNotificationReadUrl(
  notification_id: MarkNotificationReadPathParams["notification_id"],
) {
  const res = {
    method: "POST",
    url: `/api/notifications/${notification_id}/read` as const,
  };
  return res;
}

/**
 * @summary Mark Notification Read
 * {@link /api/notifications/:notification_id/read}
 */
export async function markNotificationRead(
  notification_id: MarkNotificationReadPathParams["notification_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    MarkNotificationReadMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getMarkNotificationReadUrl(notification_id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
