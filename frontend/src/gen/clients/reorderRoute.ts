/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  ReorderRouteMutationRequest,
  ReorderRouteMutationResponse,
} from "../types/ReorderRoute.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getReorderRouteUrl() {
  const res = { method: "POST", url: `/api/route/reorder` as const };
  return res;
}

/**
 * @summary Reorder Route
 * {@link /api/route/reorder}
 */
export async function reorderRoute(
  data: ReorderRouteMutationRequest,
  config: Partial<RequestConfig<ReorderRouteMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;

  const res = await request<
    ReorderRouteMutationResponse,
    ResponseErrorConfig<Error>,
    ReorderRouteMutationRequest
  >({
    method: "POST",
    url: getReorderRouteUrl().url.toString(),
    data: requestData,
    ...requestConfig,
  });
  return res.data;
}
