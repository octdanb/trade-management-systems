/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  ListEquipmentQueryResponse,
  ListEquipmentQueryParams,
} from "../types/ListEquipment.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getListEquipmentUrl() {
  const res = { method: "GET", url: `/api/equipment` as const };
  return res;
}

/**
 * @summary List Equipment
 * {@link /api/equipment}
 */
export async function listEquipment(
  params?: ListEquipmentQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ListEquipmentQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getListEquipmentUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
