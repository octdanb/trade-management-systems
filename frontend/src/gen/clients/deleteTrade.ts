/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  DeleteTradeMutationResponse,
  DeleteTradePathParams,
} from "../types/DeleteTrade.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getDeleteTradeUrl(trade_id: DeleteTradePathParams["trade_id"]) {
  const res = { method: "DELETE", url: `/api/trades/${trade_id}` as const };
  return res;
}

/**
 * @summary Delete Trade
 * {@link /api/trades/:trade_id}
 */
export async function deleteTrade(
  trade_id: DeleteTradePathParams["trade_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    DeleteTradeMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "DELETE",
    url: getDeleteTradeUrl(trade_id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
