/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  ListSeriesQueryResponse,
  ListSeriesQueryParams,
} from "../types/ListSeries.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getListSeriesUrl() {
  const res = { method: "GET", url: `/api/series` as const };
  return res;
}

/**
 * @summary List Series
 * {@link /api/series}
 */
export async function listSeries(
  params?: ListSeriesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ListSeriesQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getListSeriesUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
