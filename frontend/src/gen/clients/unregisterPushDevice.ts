/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  UnregisterPushDeviceMutationResponse,
  UnregisterPushDevicePathParams,
} from "../types/UnregisterPushDevice.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getUnregisterPushDeviceUrl(
  device_id: UnregisterPushDevicePathParams["device_id"],
) {
  const res = { method: "DELETE", url: `/api/devices/${device_id}` as const };
  return res;
}

/**
 * @summary Unregister Push Device
 * {@link /api/devices/:device_id}
 */
export async function unregisterPushDevice(
  device_id: UnregisterPushDevicePathParams["device_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    UnregisterPushDeviceMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "DELETE",
    url: getUnregisterPushDeviceUrl(device_id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
