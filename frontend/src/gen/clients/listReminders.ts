/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type { ListRemindersQueryResponse } from "../types/ListReminders.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getListRemindersUrl() {
  const res = { method: "GET", url: `/api/reminders` as const };
  return res;
}

/**
 * @description Equipment due (or nearly due) for service, computed live.
 * @summary List Reminders
 * {@link /api/reminders}
 */
export async function listReminders(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ListRemindersQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getListRemindersUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
