/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  DeleteEquipmentMutationResponse,
  DeleteEquipmentPathParams,
} from "../types/DeleteEquipment.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getDeleteEquipmentUrl(
  equipment_id: DeleteEquipmentPathParams["equipment_id"],
) {
  const res = {
    method: "DELETE",
    url: `/api/equipment/${equipment_id}` as const,
  };
  return res;
}

/**
 * @summary Delete Equipment
 * {@link /api/equipment/:equipment_id}
 */
export async function deleteEquipment(
  equipment_id: DeleteEquipmentPathParams["equipment_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    DeleteEquipmentMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "DELETE",
    url: getDeleteEquipmentUrl(equipment_id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
