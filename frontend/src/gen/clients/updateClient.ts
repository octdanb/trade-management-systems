/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  UpdateClientMutationRequest,
  UpdateClientMutationResponse,
  UpdateClientPathParams,
} from "../types/UpdateClient.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getUpdateClientUrl(client_id: UpdateClientPathParams["client_id"]) {
  const res = { method: "PUT", url: `/api/clients/${client_id}` as const };
  return res;
}

/**
 * @summary Update Client
 * {@link /api/clients/:client_id}
 */
export async function updateClient(
  client_id: UpdateClientPathParams["client_id"],
  data: UpdateClientMutationRequest,
  config: Partial<RequestConfig<UpdateClientMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;

  const res = await request<
    UpdateClientMutationResponse,
    ResponseErrorConfig<Error>,
    UpdateClientMutationRequest
  >({
    method: "PUT",
    url: getUpdateClientUrl(client_id).url.toString(),
    data: requestData,
    ...requestConfig,
  });
  return res.data;
}
