/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type { ListTradesQueryResponse } from "../types/ListTrades.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getListTradesUrl() {
  const res = { method: "GET", url: `/api/trades` as const };
  return res;
}

/**
 * @summary List Trades
 * {@link /api/trades}
 */
export async function listTrades(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ListTradesQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getListTradesUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
