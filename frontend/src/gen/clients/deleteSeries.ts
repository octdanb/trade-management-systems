/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  DeleteSeriesMutationResponse,
  DeleteSeriesPathParams,
} from "../types/DeleteSeries.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";

function getDeleteSeriesUrl(series_id: DeleteSeriesPathParams["series_id"]) {
  const res = { method: "DELETE", url: `/api/series/${series_id}` as const };
  return res;
}

/**
 * @description Delete a series and all of its occurrences (including history).
 * @summary Delete Series
 * {@link /api/series/:series_id}
 */
export async function deleteSeries(
  series_id: DeleteSeriesPathParams["series_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    DeleteSeriesMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "DELETE",
    url: getDeleteSeriesUrl(series_id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
