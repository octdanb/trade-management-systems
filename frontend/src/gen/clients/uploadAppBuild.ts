/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/kubb-client";
import type {
  UploadAppBuildMutationRequest,
  UploadAppBuildMutationResponse,
} from "../types/UploadAppBuild.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import { buildFormData } from "../.kubb/config.ts";

function getUploadAppBuildUrl() {
  const res = { method: "POST", url: `/api/app/builds` as const };
  return res;
}

/**
 * @summary Upload App Build
 * {@link /api/app/builds}
 */
export async function uploadAppBuild(
  data: UploadAppBuildMutationRequest,
  config: Partial<RequestConfig<UploadAppBuildMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;
  const formData = buildFormData(requestData);
  const res = await request<
    UploadAppBuildMutationResponse,
    ResponseErrorConfig<Error>,
    UploadAppBuildMutationRequest
  >({
    method: "POST",
    url: getUploadAppBuildUrl().url.toString(),
    data: formData as FormData,
    ...requestConfig,
  });
  return res.data;
}
