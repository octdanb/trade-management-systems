/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  OptimizeRouteMutationRequest,
  OptimizeRouteMutationResponse,
} from "../types/OptimizeRoute.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  UseMutationOptions,
  UseMutationResult,
  QueryClient,
} from "@tanstack/react-query";
import { optimizeRoute } from "../clients/optimizeRoute.ts";
import { mutationOptions, useMutation } from "@tanstack/react-query";

export const optimizeRouteMutationKey = () =>
  [{ url: "/api/route/optimize" }] as const;

export type OptimizeRouteMutationKey = ReturnType<
  typeof optimizeRouteMutationKey
>;

export function optimizeRouteMutationOptions<TContext = unknown>(
  config: Partial<RequestConfig<OptimizeRouteMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const mutationKey = optimizeRouteMutationKey();
  return mutationOptions<
    OptimizeRouteMutationResponse,
    ResponseErrorConfig<Error>,
    { data: OptimizeRouteMutationRequest },
    TContext
  >({
    mutationKey,
    mutationFn: async ({ data }) => {
      return optimizeRoute(data, config);
    },
  });
}

/**
 * @summary Optimize Route
 * {@link /api/route/optimize}
 */
export function useOptimizeRoute<TContext>(
  options: {
    mutation?: UseMutationOptions<
      OptimizeRouteMutationResponse,
      ResponseErrorConfig<Error>,
      { data: OptimizeRouteMutationRequest },
      TContext
    > & { client?: QueryClient };
    client?: Partial<RequestConfig<OptimizeRouteMutationRequest>> & {
      client?: Client;
    };
  } = {},
) {
  const { mutation = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...mutationOptions } = mutation;
  const mutationKey = mutationOptions.mutationKey ?? optimizeRouteMutationKey();

  const baseOptions = optimizeRouteMutationOptions(
    config,
  ) as UseMutationOptions<
    OptimizeRouteMutationResponse,
    ResponseErrorConfig<Error>,
    { data: OptimizeRouteMutationRequest },
    TContext
  >;

  return useMutation<
    OptimizeRouteMutationResponse,
    ResponseErrorConfig<Error>,
    { data: OptimizeRouteMutationRequest },
    TContext
  >(
    {
      ...baseOptions,
      mutationKey,
      ...mutationOptions,
    },
    queryClient,
  ) as UseMutationResult<
    OptimizeRouteMutationResponse,
    ResponseErrorConfig<Error>,
    { data: OptimizeRouteMutationRequest },
    TContext
  >;
}
