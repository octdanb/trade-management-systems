/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetRoutePlanQueryResponse,
  GetRoutePlanQueryParams,
} from "../types/GetRoutePlan.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { getRoutePlan } from "../clients/getRoutePlan.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getRoutePlanSuspenseQueryKey = (params: GetRoutePlanQueryParams) =>
  [{ url: "/api/route" }, ...(params ? [params] : [])] as const;

export type GetRoutePlanSuspenseQueryKey = ReturnType<
  typeof getRoutePlanSuspenseQueryKey
>;

export function getRoutePlanSuspenseQueryOptions(
  params: GetRoutePlanQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getRoutePlanSuspenseQueryKey(params);
  return queryOptions<
    GetRoutePlanQueryResponse,
    ResponseErrorConfig<Error>,
    GetRoutePlanQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getRoutePlan(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary Get Route Plan
 * {@link /api/route}
 */
export function useGetRoutePlanSuspense<
  TData = GetRoutePlanQueryResponse,
  TQueryKey extends QueryKey = GetRoutePlanSuspenseQueryKey,
>(
  params: GetRoutePlanQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetRoutePlanQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getRoutePlanSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getRoutePlanSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
