/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ListEquipmentQueryResponse,
  ListEquipmentQueryParams,
} from "../types/ListEquipment.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { listEquipment } from "../clients/listEquipment.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const listEquipmentSuspenseQueryKey = (
  params?: ListEquipmentQueryParams,
) => [{ url: "/api/equipment" }, ...(params ? [params] : [])] as const;

export type ListEquipmentSuspenseQueryKey = ReturnType<
  typeof listEquipmentSuspenseQueryKey
>;

export function listEquipmentSuspenseQueryOptions(
  params?: ListEquipmentQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listEquipmentSuspenseQueryKey(params);
  return queryOptions<
    ListEquipmentQueryResponse,
    ResponseErrorConfig<Error>,
    ListEquipmentQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listEquipment(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List Equipment
 * {@link /api/equipment}
 */
export function useListEquipmentSuspense<
  TData = ListEquipmentQueryResponse,
  TQueryKey extends QueryKey = ListEquipmentSuspenseQueryKey,
>(
  params?: ListEquipmentQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListEquipmentQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? listEquipmentSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...listEquipmentSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
