/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetEquipmentQueryResponse,
  GetEquipmentPathParams,
} from "../types/GetEquipment.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getEquipment } from "../clients/getEquipment.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getEquipmentQueryKey = (
  equipment_id: GetEquipmentPathParams["equipment_id"] | undefined,
) =>
  [
    {
      url: "/api/equipment/:equipment_id",
      params: { equipment_id: equipment_id },
    },
  ] as const;

export type GetEquipmentQueryKey = ReturnType<typeof getEquipmentQueryKey>;

export function getEquipmentQueryOptions(
  equipment_id: GetEquipmentPathParams["equipment_id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getEquipmentQueryKey(equipment_id);
  return queryOptions<
    GetEquipmentQueryResponse,
    ResponseErrorConfig<Error>,
    GetEquipmentQueryResponse,
    typeof queryKey
  >({
    enabled: !!equipment_id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getEquipment(equipment_id!, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary Get Equipment
 * {@link /api/equipment/:equipment_id}
 */
export function useGetEquipment<
  TData = GetEquipmentQueryResponse,
  TQueryData = GetEquipmentQueryResponse,
  TQueryKey extends QueryKey = GetEquipmentQueryKey,
>(
  equipment_id: GetEquipmentPathParams["equipment_id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetEquipmentQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getEquipmentQueryKey(equipment_id);

  const query = useQuery(
    {
      ...getEquipmentQueryOptions(equipment_id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
