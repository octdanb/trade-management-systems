/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ListSeriesQueryResponse,
  ListSeriesQueryParams,
} from "../types/ListSeries.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { listSeries } from "../clients/listSeries.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const listSeriesSuspenseQueryKey = (params?: ListSeriesQueryParams) =>
  [{ url: "/api/series" }, ...(params ? [params] : [])] as const;

export type ListSeriesSuspenseQueryKey = ReturnType<
  typeof listSeriesSuspenseQueryKey
>;

export function listSeriesSuspenseQueryOptions(
  params?: ListSeriesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listSeriesSuspenseQueryKey(params);
  return queryOptions<
    ListSeriesQueryResponse,
    ResponseErrorConfig<Error>,
    ListSeriesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listSeries(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List Series
 * {@link /api/series}
 */
export function useListSeriesSuspense<
  TData = ListSeriesQueryResponse,
  TQueryKey extends QueryKey = ListSeriesSuspenseQueryKey,
>(
  params?: ListSeriesQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListSeriesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? listSeriesSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...listSeriesSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
