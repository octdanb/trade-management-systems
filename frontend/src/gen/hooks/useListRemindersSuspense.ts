/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { ListRemindersQueryResponse } from "../types/ListReminders.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { listReminders } from "../clients/listReminders.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const listRemindersSuspenseQueryKey = () =>
  [{ url: "/api/reminders" }] as const;

export type ListRemindersSuspenseQueryKey = ReturnType<
  typeof listRemindersSuspenseQueryKey
>;

export function listRemindersSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listRemindersSuspenseQueryKey();
  return queryOptions<
    ListRemindersQueryResponse,
    ResponseErrorConfig<Error>,
    ListRemindersQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listReminders({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Equipment due (or nearly due) for service, computed live.
 * @summary List Reminders
 * {@link /api/reminders}
 */
export function useListRemindersSuspense<
  TData = ListRemindersQueryResponse,
  TQueryKey extends QueryKey = ListRemindersSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListRemindersQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listRemindersSuspenseQueryKey();

  const query = useSuspenseQuery(
    {
      ...listRemindersSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
