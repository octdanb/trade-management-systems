/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ListJobsQueryResponse,
  ListJobsQueryParams,
} from "../types/ListJobs.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { listJobs } from "../clients/listJobs.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const listJobsSuspenseQueryKey = (params: ListJobsQueryParams) =>
  [{ url: "/api/jobs" }, ...(params ? [params] : [])] as const;

export type ListJobsSuspenseQueryKey = ReturnType<
  typeof listJobsSuspenseQueryKey
>;

export function listJobsSuspenseQueryOptions(
  params: ListJobsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listJobsSuspenseQueryKey(params);
  return queryOptions<
    ListJobsQueryResponse,
    ResponseErrorConfig<Error>,
    ListJobsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listJobs(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List Jobs
 * {@link /api/jobs}
 */
export function useListJobsSuspense<
  TData = ListJobsQueryResponse,
  TQueryKey extends QueryKey = ListJobsSuspenseQueryKey,
>(
  params: ListJobsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListJobsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? listJobsSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...listJobsSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
