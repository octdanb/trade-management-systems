/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { ListTradesQueryResponse } from "../types/ListTrades.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { listTrades } from "../clients/listTrades.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const listTradesSuspenseQueryKey = () =>
  [{ url: "/api/trades" }] as const;

export type ListTradesSuspenseQueryKey = ReturnType<
  typeof listTradesSuspenseQueryKey
>;

export function listTradesSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listTradesSuspenseQueryKey();
  return queryOptions<
    ListTradesQueryResponse,
    ResponseErrorConfig<Error>,
    ListTradesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listTrades({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List Trades
 * {@link /api/trades}
 */
export function useListTradesSuspense<
  TData = ListTradesQueryResponse,
  TQueryKey extends QueryKey = ListTradesSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListTradesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listTradesSuspenseQueryKey();

  const query = useSuspenseQuery(
    {
      ...listTradesSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
