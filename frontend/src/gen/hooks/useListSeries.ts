/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ListSeriesQueryResponse,
  ListSeriesQueryParams,
} from "../types/ListSeries.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { listSeries } from "../clients/listSeries.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listSeriesQueryKey = (params?: ListSeriesQueryParams) =>
  [{ url: "/api/series" }, ...(params ? [params] : [])] as const;

export type ListSeriesQueryKey = ReturnType<typeof listSeriesQueryKey>;

export function listSeriesQueryOptions(
  params?: ListSeriesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listSeriesQueryKey(params);
  return queryOptions<
    ListSeriesQueryResponse,
    ResponseErrorConfig<Error>,
    ListSeriesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listSeries(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List Series
 * {@link /api/series}
 */
export function useListSeries<
  TData = ListSeriesQueryResponse,
  TQueryData = ListSeriesQueryResponse,
  TQueryKey extends QueryKey = ListSeriesQueryKey,
>(
  params?: ListSeriesQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListSeriesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listSeriesQueryKey(params);

  const query = useQuery(
    {
      ...listSeriesQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
