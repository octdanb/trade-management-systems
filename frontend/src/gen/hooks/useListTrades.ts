/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { ListTradesQueryResponse } from "../types/ListTrades.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { listTrades } from "../clients/listTrades.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listTradesQueryKey = () => [{ url: "/api/trades" }] as const;

export type ListTradesQueryKey = ReturnType<typeof listTradesQueryKey>;

export function listTradesQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listTradesQueryKey();
  return queryOptions<
    ListTradesQueryResponse,
    ResponseErrorConfig<Error>,
    ListTradesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listTrades({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List Trades
 * {@link /api/trades}
 */
export function useListTrades<
  TData = ListTradesQueryResponse,
  TQueryData = ListTradesQueryResponse,
  TQueryKey extends QueryKey = ListTradesQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListTradesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listTradesQueryKey();

  const query = useQuery(
    {
      ...listTradesQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
