/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type { RunRemindersMutationResponse } from "../types/RunReminders.ts";

function getRunRemindersUrl() {
  const res = { method: "POST", url: `/api/reminders/run` as const };
  return res;
}

/**
 * @description Trigger reminder generation for the current user now (what the daily
 * `send_due_reminders` job does). Returns the notifications it created.
 * @summary Run Reminders
 * {@link /api/reminders/run}
 */
export async function runReminders(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    RunRemindersMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getRunRemindersUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
