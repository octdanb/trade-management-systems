/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  GeocodeClientMutationResponse,
  GeocodeClientPathParams,
} from "../types/GeocodeClient.ts";

function getGeocodeClientUrl(client_id: GeocodeClientPathParams["client_id"]) {
  const res = {
    method: "POST",
    url: `/api/clients/${client_id}/geocode` as const,
  };
  return res;
}

/**
 * @summary Geocode Client Retry
 * {@link /api/clients/:client_id/geocode}
 */
export async function geocodeClient(
  client_id: GeocodeClientPathParams["client_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GeocodeClientMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getGeocodeClientUrl(client_id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
