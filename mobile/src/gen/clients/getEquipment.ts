/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  GetEquipmentQueryResponse,
  GetEquipmentPathParams,
} from "../types/GetEquipment.ts";

function getGetEquipmentUrl(
  equipment_id: GetEquipmentPathParams["equipment_id"],
) {
  const res = { method: "GET", url: `/api/equipment/${equipment_id}` as const };
  return res;
}

/**
 * @summary Get Equipment
 * {@link /api/equipment/:equipment_id}
 */
export async function getEquipment(
  equipment_id: GetEquipmentPathParams["equipment_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetEquipmentQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetEquipmentUrl(equipment_id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
