/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  ListClientsQueryResponse,
  ListClientsQueryParams,
} from "../types/ListClients.ts";

function getListClientsUrl() {
  const res = { method: "GET", url: `/api/clients` as const };
  return res;
}

/**
 * @summary List Clients
 * {@link /api/clients}
 */
export async function listClients(
  params?: ListClientsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ListClientsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getListClientsUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
