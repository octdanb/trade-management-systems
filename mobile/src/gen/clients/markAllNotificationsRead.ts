/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type { MarkAllNotificationsReadMutationResponse } from "../types/MarkAllNotificationsRead.ts";

function getMarkAllNotificationsReadUrl() {
  const res = { method: "POST", url: `/api/notifications/read-all` as const };
  return res;
}

/**
 * @summary Mark All Notifications Read
 * {@link /api/notifications/read-all}
 */
export async function markAllNotificationsRead(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    MarkAllNotificationsReadMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getMarkAllNotificationsReadUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
