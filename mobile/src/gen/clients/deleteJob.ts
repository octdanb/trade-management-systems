/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  DeleteJobMutationResponse,
  DeleteJobPathParams,
} from "../types/DeleteJob.ts";

function getDeleteJobUrl(job_id: DeleteJobPathParams["job_id"]) {
  const res = { method: "DELETE", url: `/api/jobs/${job_id}` as const };
  return res;
}

/**
 * @summary Delete Job
 * {@link /api/jobs/:job_id}
 */
export async function deleteJob(
  job_id: DeleteJobPathParams["job_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    DeleteJobMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "DELETE",
    url: getDeleteJobUrl(job_id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
