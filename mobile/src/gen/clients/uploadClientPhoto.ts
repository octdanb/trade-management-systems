/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  UploadClientPhotoMutationRequest,
  UploadClientPhotoMutationResponse,
  UploadClientPhotoPathParams,
} from "../types/UploadClientPhoto.ts";
import { buildFormData } from "../.kubb/config.ts";

function getUploadClientPhotoUrl(
  client_id: UploadClientPhotoPathParams["client_id"],
) {
  const res = {
    method: "POST",
    url: `/api/clients/${client_id}/photos` as const,
  };
  return res;
}

/**
 * @summary Upload Client Photo
 * {@link /api/clients/:client_id/photos}
 */
export async function uploadClientPhoto(
  client_id: UploadClientPhotoPathParams["client_id"],
  data: UploadClientPhotoMutationRequest,
  config: Partial<RequestConfig<UploadClientPhotoMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;
  const formData = buildFormData(requestData);
  const res = await request<
    UploadClientPhotoMutationResponse,
    ResponseErrorConfig<Error>,
    UploadClientPhotoMutationRequest
  >({
    method: "POST",
    url: getUploadClientPhotoUrl(client_id).url.toString(),
    data: formData as FormData,
    ...requestConfig,
  });
  return res.data;
}
