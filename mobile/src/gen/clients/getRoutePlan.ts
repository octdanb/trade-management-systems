/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  GetRoutePlanQueryResponse,
  GetRoutePlanQueryParams,
} from "../types/GetRoutePlan.ts";

function getGetRoutePlanUrl() {
  const res = { method: "GET", url: `/api/route` as const };
  return res;
}

/**
 * @summary Get Route Plan
 * {@link /api/route}
 */
export async function getRoutePlan(
  params: GetRoutePlanQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetRoutePlanQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetRoutePlanUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
