/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  OptimizeRouteMutationRequest,
  OptimizeRouteMutationResponse,
} from "../types/OptimizeRoute.ts";

function getOptimizeRouteUrl() {
  const res = { method: "POST", url: `/api/route/optimize` as const };
  return res;
}

/**
 * @summary Optimize Route
 * {@link /api/route/optimize}
 */
export async function optimizeRoute(
  data: OptimizeRouteMutationRequest,
  config: Partial<RequestConfig<OptimizeRouteMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;

  const res = await request<
    OptimizeRouteMutationResponse,
    ResponseErrorConfig<Error>,
    OptimizeRouteMutationRequest
  >({
    method: "POST",
    url: getOptimizeRouteUrl().url.toString(),
    data: requestData,
    ...requestConfig,
  });
  return res.data;
}
