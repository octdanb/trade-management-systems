/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../lib/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  GetClientQueryResponse,
  GetClientPathParams,
} from "../types/GetClient.ts";

function getGetClientUrl(client_id: GetClientPathParams["client_id"]) {
  const res = { method: "GET", url: `/api/clients/${client_id}` as const };
  return res;
}

/**
 * @summary Get Client
 * {@link /api/clients/:client_id}
 */
export async function getClient(
  client_id: GetClientPathParams["client_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetClientQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetClientUrl(client_id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
