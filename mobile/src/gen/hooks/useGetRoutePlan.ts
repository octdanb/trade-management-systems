/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  GetRoutePlanQueryResponse,
  GetRoutePlanQueryParams,
} from "../types/GetRoutePlan.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getRoutePlan } from "../clients/getRoutePlan.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getRoutePlanQueryKey = (params: GetRoutePlanQueryParams) =>
  [{ url: "/api/route" }, ...(params ? [params] : [])] as const;

export type GetRoutePlanQueryKey = ReturnType<typeof getRoutePlanQueryKey>;

export function getRoutePlanQueryOptions(
  params: GetRoutePlanQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getRoutePlanQueryKey(params);
  return queryOptions<
    GetRoutePlanQueryResponse,
    ResponseErrorConfig<Error>,
    GetRoutePlanQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getRoutePlan(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary Get Route Plan
 * {@link /api/route}
 */
export function useGetRoutePlan<
  TData = GetRoutePlanQueryResponse,
  TQueryData = GetRoutePlanQueryResponse,
  TQueryKey extends QueryKey = GetRoutePlanQueryKey,
>(
  params: GetRoutePlanQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetRoutePlanQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getRoutePlanQueryKey(params);

  const query = useQuery(
    {
      ...getRoutePlanQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
