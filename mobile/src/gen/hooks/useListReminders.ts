/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type { ListRemindersQueryResponse } from "../types/ListReminders.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { listReminders } from "../clients/listReminders.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listRemindersQueryKey = () => [{ url: "/api/reminders" }] as const;

export type ListRemindersQueryKey = ReturnType<typeof listRemindersQueryKey>;

export function listRemindersQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listRemindersQueryKey();
  return queryOptions<
    ListRemindersQueryResponse,
    ResponseErrorConfig<Error>,
    ListRemindersQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listReminders({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Equipment due (or nearly due) for service, computed live.
 * @summary List Reminders
 * {@link /api/reminders}
 */
export function useListReminders<
  TData = ListRemindersQueryResponse,
  TQueryData = ListRemindersQueryResponse,
  TQueryKey extends QueryKey = ListRemindersQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListRemindersQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listRemindersQueryKey();

  const query = useQuery(
    {
      ...listRemindersQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
