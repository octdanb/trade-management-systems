/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  GetEquipmentQueryResponse,
  GetEquipmentPathParams,
} from "../types/GetEquipment.ts";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { getEquipment } from "../clients/getEquipment.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getEquipmentSuspenseQueryKey = (
  equipment_id: GetEquipmentPathParams["equipment_id"] | undefined,
) =>
  [
    {
      url: "/api/equipment/:equipment_id",
      params: { equipment_id: equipment_id },
    },
  ] as const;

export type GetEquipmentSuspenseQueryKey = ReturnType<
  typeof getEquipmentSuspenseQueryKey
>;

export function getEquipmentSuspenseQueryOptions(
  equipment_id: GetEquipmentPathParams["equipment_id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getEquipmentSuspenseQueryKey(equipment_id);
  return queryOptions<
    GetEquipmentQueryResponse,
    ResponseErrorConfig<Error>,
    GetEquipmentQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getEquipment(equipment_id, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary Get Equipment
 * {@link /api/equipment/:equipment_id}
 */
export function useGetEquipmentSuspense<
  TData = GetEquipmentQueryResponse,
  TQueryKey extends QueryKey = GetEquipmentSuspenseQueryKey,
>(
  equipment_id: GetEquipmentPathParams["equipment_id"],
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetEquipmentQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getEquipmentSuspenseQueryKey(equipment_id);

  const query = useSuspenseQuery(
    {
      ...getEquipmentSuspenseQueryOptions(equipment_id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
