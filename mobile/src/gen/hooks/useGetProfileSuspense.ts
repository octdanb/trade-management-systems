/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type { GetProfileQueryResponse } from "../types/GetProfile.ts";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { getProfile } from "../clients/getProfile.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getProfileSuspenseQueryKey = () =>
  [{ url: "/api/profile" }] as const;

export type GetProfileSuspenseQueryKey = ReturnType<
  typeof getProfileSuspenseQueryKey
>;

export function getProfileSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getProfileSuspenseQueryKey();
  return queryOptions<
    GetProfileQueryResponse,
    ResponseErrorConfig<Error>,
    GetProfileQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getProfile({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary Get Profile
 * {@link /api/profile}
 */
export function useGetProfileSuspense<
  TData = GetProfileQueryResponse,
  TQueryKey extends QueryKey = GetProfileSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetProfileQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getProfileSuspenseQueryKey();

  const query = useSuspenseQuery(
    {
      ...getProfileSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
