/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  ListClientsQueryResponse,
  ListClientsQueryParams,
} from "../types/ListClients.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { listClients } from "../clients/listClients.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listClientsQueryKey = (params?: ListClientsQueryParams) =>
  [{ url: "/api/clients" }, ...(params ? [params] : [])] as const;

export type ListClientsQueryKey = ReturnType<typeof listClientsQueryKey>;

export function listClientsQueryOptions(
  params?: ListClientsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listClientsQueryKey(params);
  return queryOptions<
    ListClientsQueryResponse,
    ResponseErrorConfig<Error>,
    ListClientsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listClients(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List Clients
 * {@link /api/clients}
 */
export function useListClients<
  TData = ListClientsQueryResponse,
  TQueryData = ListClientsQueryResponse,
  TQueryKey extends QueryKey = ListClientsQueryKey,
>(
  params?: ListClientsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListClientsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listClientsQueryKey(params);

  const query = useQuery(
    {
      ...listClientsQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
