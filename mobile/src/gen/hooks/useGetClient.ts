/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  GetClientQueryResponse,
  GetClientPathParams,
} from "../types/GetClient.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getClient } from "../clients/getClient.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getClientQueryKey = (
  client_id: GetClientPathParams["client_id"] | undefined,
) =>
  [
    { url: "/api/clients/:client_id", params: { client_id: client_id } },
  ] as const;

export type GetClientQueryKey = ReturnType<typeof getClientQueryKey>;

export function getClientQueryOptions(
  client_id: GetClientPathParams["client_id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getClientQueryKey(client_id);
  return queryOptions<
    GetClientQueryResponse,
    ResponseErrorConfig<Error>,
    GetClientQueryResponse,
    typeof queryKey
  >({
    enabled: !!client_id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getClient(client_id!, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary Get Client
 * {@link /api/clients/:client_id}
 */
export function useGetClient<
  TData = GetClientQueryResponse,
  TQueryData = GetClientQueryResponse,
  TQueryKey extends QueryKey = GetClientQueryKey,
>(
  client_id: GetClientPathParams["client_id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetClientQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getClientQueryKey(client_id);

  const query = useQuery(
    {
      ...getClientQueryOptions(client_id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
