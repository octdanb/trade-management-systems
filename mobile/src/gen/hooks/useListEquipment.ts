/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  ListEquipmentQueryResponse,
  ListEquipmentQueryParams,
} from "../types/ListEquipment.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { listEquipment } from "../clients/listEquipment.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listEquipmentQueryKey = (params?: ListEquipmentQueryParams) =>
  [{ url: "/api/equipment" }, ...(params ? [params] : [])] as const;

export type ListEquipmentQueryKey = ReturnType<typeof listEquipmentQueryKey>;

export function listEquipmentQueryOptions(
  params?: ListEquipmentQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listEquipmentQueryKey(params);
  return queryOptions<
    ListEquipmentQueryResponse,
    ResponseErrorConfig<Error>,
    ListEquipmentQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listEquipment(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List Equipment
 * {@link /api/equipment}
 */
export function useListEquipment<
  TData = ListEquipmentQueryResponse,
  TQueryData = ListEquipmentQueryResponse,
  TQueryKey extends QueryKey = ListEquipmentQueryKey,
>(
  params?: ListEquipmentQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListEquipmentQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listEquipmentQueryKey(params);

  const query = useQuery(
    {
      ...listEquipmentQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
