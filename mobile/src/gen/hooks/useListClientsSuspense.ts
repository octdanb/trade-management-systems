/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type {
  ListClientsQueryResponse,
  ListClientsQueryParams,
} from "../types/ListClients.ts";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { listClients } from "../clients/listClients.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const listClientsSuspenseQueryKey = (params?: ListClientsQueryParams) =>
  [{ url: "/api/clients" }, ...(params ? [params] : [])] as const;

export type ListClientsSuspenseQueryKey = ReturnType<
  typeof listClientsSuspenseQueryKey
>;

export function listClientsSuspenseQueryOptions(
  params?: ListClientsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listClientsSuspenseQueryKey(params);
  return queryOptions<
    ListClientsQueryResponse,
    ResponseErrorConfig<Error>,
    ListClientsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listClients(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List Clients
 * {@link /api/clients}
 */
export function useListClientsSuspense<
  TData = ListClientsQueryResponse,
  TQueryKey extends QueryKey = ListClientsSuspenseQueryKey,
>(
  params?: ListClientsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListClientsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? listClientsSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...listClientsSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
