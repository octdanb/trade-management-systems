/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../lib/kubb-client";
import type { RunRemindersMutationResponse } from "../types/RunReminders.ts";
import type {
  UseMutationOptions,
  UseMutationResult,
  QueryClient,
} from "@tanstack/react-query";
import { runReminders } from "../clients/runReminders.ts";
import { mutationOptions, useMutation } from "@tanstack/react-query";

export const runRemindersMutationKey = () =>
  [{ url: "/api/reminders/run" }] as const;

export type RunRemindersMutationKey = ReturnType<
  typeof runRemindersMutationKey
>;

export function runRemindersMutationOptions<TContext = unknown>(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const mutationKey = runRemindersMutationKey();
  return mutationOptions<
    RunRemindersMutationResponse,
    ResponseErrorConfig<Error>,
    void,
    TContext
  >({
    mutationKey,
    mutationFn: async () => {
      return runReminders(config);
    },
  });
}

/**
 * @description Trigger reminder generation for the current user now (what the daily
 * `send_due_reminders` job does). Returns the notifications it created.
 * @summary Run Reminders
 * {@link /api/reminders/run}
 */
export function useRunReminders<TContext>(
  options: {
    mutation?: UseMutationOptions<
      RunRemindersMutationResponse,
      ResponseErrorConfig<Error>,
      void,
      TContext
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { mutation = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...mutationOptions } = mutation;
  const mutationKey = mutationOptions.mutationKey ?? runRemindersMutationKey();

  const baseOptions = runRemindersMutationOptions(config) as UseMutationOptions<
    RunRemindersMutationResponse,
    ResponseErrorConfig<Error>,
    void,
    TContext
  >;

  return useMutation<
    RunRemindersMutationResponse,
    ResponseErrorConfig<Error>,
    void,
    TContext
  >(
    {
      ...baseOptions,
      mutationKey,
      ...mutationOptions,
    },
    queryClient,
  ) as UseMutationResult<
    RunRemindersMutationResponse,
    ResponseErrorConfig<Error>,
    void,
    TContext
  >;
}
